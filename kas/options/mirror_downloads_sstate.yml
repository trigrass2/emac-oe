header:
  version: 12

local_conf_header:
  downloads: |
    SOURCE_MIRROR_URL ?= "file:///srv/yocto/downloads/"
    INHERIT:append = " own-mirrors "
  sstate_cache: |
    SSTATE_MIRRORS ?= "\
    file://.* file:///srv/yocto/sstate-cache/PATH \
    "
