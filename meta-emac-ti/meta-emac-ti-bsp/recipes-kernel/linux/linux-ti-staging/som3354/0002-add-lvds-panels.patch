diff --git a/drivers/gpu/drm/panel/panel-simple.c b/drivers/gpu/drm/panel/panel-simple.c
index 1c23bc65a105..bec9a21949e1 100644
--- a/drivers/gpu/drm/panel/panel-simple.c
+++ b/drivers/gpu/drm/panel/panel-simple.c
@@ -3117,6 +3117,32 @@ static const struct panel_desc osddisplays_osd070t1718_19ts = {
 	.connector_type = DRM_MODE_CONNECTOR_DPI,
 };
 
+static const struct drm_display_mode osd101t1515_45ts_mode = {
+	.clock = 51450,
+        .hdisplay = 1024,
+        .hsync_start = 1024 + 156,
+        .hsync_end = 1024 + 156 + 8,
+        .htotal = 1024 + 156 + 8 + 156,
+	.vdisplay = 600,
+	.vsync_start = 600 + 16,
+	.vsync_end = 600 + 16 + 6,
+	.vtotal = 600 + 16 + 6 + 16,
+	.flags = DRM_MODE_FLAG_NVSYNC | DRM_MODE_FLAG_NHSYNC,
+};
+
+static const struct panel_desc osd101t1515_45ts = {
+	.modes = &osd101t1515_45ts_mode,
+	.num_modes = 1,
+	.bpc = 8,
+	.size = {
+		.width = 223,
+		.height = 125,
+	},
+        .bus_format = MEDIA_BUS_FMT_RGB888_1X7X4_JEIDA,
+        .bus_flags = DRM_BUS_FLAG_DE_HIGH | DRM_BUS_FLAG_DATA_MSB_TO_LSB,
+        .connector_type = DRM_MODE_CONNECTOR_LVDS,
+};
+
 static const struct drm_display_mode pda_91_00156_a0_mode = {
 	.clock = 33300,
 	.hdisplay = 800,
@@ -3390,6 +3416,37 @@ static const struct panel_desc sharp_lq070y3dg3b = {
 		     DRM_BUS_FLAG_SYNC_DRIVE_POSEDGE,
 };
 
+
+
+static const struct drm_display_mode fema_ttx2nlwhtl_mode = {
+	.clock = 33260,
+	.hdisplay = 800,
+	.hsync_start = 800 + 42,
+	.hsync_end = 800 + 42 + 128,
+	.htotal = 800 + 42 + 128 + 86,
+	.vdisplay = 480,
+	.vsync_start = 480 + 10,
+	.vsync_end = 480 + 10 + 2,
+	.vtotal = 480 + 10 + 2 + 33,
+	.flags = DRM_MODE_FLAG_NVSYNC | DRM_MODE_FLAG_NHSYNC,
+};
+static const struct panel_desc fema_ttx2nlwhtl = {
+	.modes = &fema_ttx2nlwhtl_mode,
+	.num_modes = 1,
+	.bpc = 8,
+	.size = {
+		.width = 152,	/* 152.4mm */
+		.height = 91,	/* 91.4mm */
+	},
+	.bus_format = MEDIA_BUS_FMT_RGB888_1X7X4_JEIDA,
+	.bus_flags = DRM_BUS_FLAG_DE_HIGH | DRM_BUS_FLAG_DATA_MSB_TO_LSB,
+	.connector_type = DRM_MODE_CONNECTOR_LVDS,
+};
+
+
+
+
+
 static const struct drm_display_mode sharp_lq035q7db03_mode = {
 	.clock = 5500,
 	.hdisplay = 240,
@@ -4198,6 +4255,9 @@ static const struct of_device_id platform_of_match[] = {
 		.compatible = "osddisplays,osd070t1718-19ts",
 		.data = &osddisplays_osd070t1718_19ts,
 	}, {
+		.compatible = "osddisplays,osd101t1515_45ts",
+		.data = &osd101t1515_45ts,
+	} ,{
 		.compatible = "pda,91-00156-a0",
 		.data = &pda_91_00156_a0,
 	}, {
@@ -4272,6 +4332,9 @@ static const struct of_device_id platform_of_match[] = {
 	}, {
 		.compatible = "toshiba,lt089ac29000",
 		.data = &toshiba_lt089ac29000,
+	}, {
+		.compatible = "fema,ttx2nlwhtl",
+		.data = &fema_ttx2nlwhtl,
 	}, {
 		.compatible = "tpk,f07a-0102",
 		.data = &tpk_f07a_0102,
