diff --git a/libopkg/opkg_remove.c b/libopkg/opkg_remove.c
index 1c3840e..63907c2 100644
--- a/libopkg/opkg_remove.c
+++ b/libopkg/opkg_remove.c
@@ -387,7 +387,10 @@ remove_data_files_and_list(pkg_t *pkg)
 
 	  if (!conf->noaction) {
 	  	opkg_msg(INFO, "Deleting %s.\n", file_name);
-	       unlink(file_name);
+	        if (unlink(file_name) == -1){
+		    opkg_msg(ERROR, "Cannot delete %s.\n", file_name);
+		    goto out1;
+		}
 	  } else
 	  	opkg_msg(INFO, "Not deleting %s. (noaction)\n",
 				file_name);
@@ -426,6 +429,7 @@ remove_data_files_and_list(pkg_t *pkg)
 	  }
      }
 
+out1:
      /* cleanup */
      while (!void_list_empty(&installed_dirs)) {
         iter = str_list_pop(&installed_dirs);
